name: Update All Fork Repositories

# 同一份文件部署到多个账号的仓库时,自动识别当前账号专属的 AUTH_PAT:
# secret 命名规则固定为 AUTH_PAT_<账号名大写>,例如 timeflysoon → AUTH_PAT_TIMEFLYSOON,
# 88alt → AUTH_PAT_88ALT。脚本用 $GITHUB_REPOSITORY_OWNER(当前仓库所属账号)自动拼出
# 这个名字去查找,不需要在文件里写死任何账号名,复制到别的账号仓库也能直接用。

on:
  schedule:
    # 北京时间 07:20 (UTC 23:20) 和 19:20 (UTC 11:20)
    - cron: '20 11,23 * * *'
  workflow_dispatch:
    inputs:
      repositories:
        description: |
          要更新的仓库：
          留空 - 仅更新当前仓库
          all - 更新所有 fork 仓库
          指定 - owner/repo,owner2/repo2
        required: false
        default: "all"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  update-repos:
    runs-on: ubuntu-latest

    steps:
      - name: 解析当前账号专属 Token
        id: resolve-token
        shell: bash
        env:
          ALL_SECRETS: ${{ toJSON(secrets) }}
        run: |
          set -Eeuo pipefail

          OWNER="$GITHUB_REPOSITORY_OWNER"
          # 账号名转大写并把非字母数字字符替换成下划线,拼出该账号专属 secret 名
          OWNER_KEY=$(printf '%s' "$OWNER" | tr '[:lower:]' '[:upper:]' | tr -cs '[:alnum:]' '_')
          SECRET_NAME="AUTH_PAT_${OWNER_KEY}"

          TOKEN=$(printf '%s' "$ALL_SECRETS" | jq -r --arg k "$SECRET_NAME" '.[$k] // empty')

          if [ -z "$TOKEN" ]; then
            echo "❌ 未找到 secret ${SECRET_NAME}"
            echo "请在本仓库 Settings → Secrets and variables → Actions 中添加,值为 ${OWNER} 账号的 classic PAT(需 repo 权限)"
            exit 1
          fi

          echo "::add-mask::$TOKEN"
          echo "GH_TOKEN=$TOKEN" >> "$GITHUB_ENV"
          echo "✅ 已识别账号 ${OWNER} 的专属 Token(${SECRET_NAME})"

      - name: 安装依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      - name: 确定更新模式
        id: determine-mode
        run: |
          echo "触发事件: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "schedule" ]; then
            echo "🕐 定时任务触发，使用 'all' 模式"
            echo "mode=all" >> $GITHUB_OUTPUT
          else
            USER_INPUT="${{ github.event.inputs.repositories }}"
            echo "👆 手动触发，用户输入: '$USER_INPUT'"
            if [ -z "$USER_INPUT" ]; then
              echo "用户输入为空，更新当前仓库"
              echo "mode=current" >> $GITHUB_OUTPUT
            elif [ "$USER_INPUT" = "all" ]; then
              echo "用户输入 'all'，更新所有 fork 仓库"
              echo "mode=all" >> $GITHUB_OUTPUT
            else
              echo "用户指定仓库: $USER_INPUT"
              echo "mode=manual" >> $GITHUB_OUTPUT
            fi
          fi

      - name: 获取所有 fork 仓库列表（all 模式）
        if: steps.determine-mode.outputs.mode == 'all'
        id: get-all-fork-repos
        run: |
          echo "🔍 正在获取您所有的 fork 仓库..."
          page=1
          ALL_FORK_REPOS=""

          while true; do
            echo "获取第 $page 页..."
            RESPONSE=$(curl -s -m 15 --retry 3 -w "\n%{http_code}" \
                          -H "Authorization: token $GH_TOKEN" \
                          "https://api.github.com/user/repos?per_page=100&page=$page&type=owner&sort=full_name")

            HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
            BODY=$(echo "$RESPONSE" | sed '$d')

            if [ "$HTTP_CODE" != "200" ]; then
              echo "❌ API 请求失败 (状态码: $HTTP_CODE)"
              break
            fi

            FORK_REPOS=$(echo "$BODY" | jq -r '.[] | select(.fork == true) | .full_name' | tr '\n' ',')

            if [ -n "$FORK_REPOS" ]; then
              ALL_FORK_REPOS="${ALL_FORK_REPOS}${FORK_REPOS}"
              REPO_COUNT=$(echo "$FORK_REPOS" | tr ',' '\n' | sed '/^$/d' | wc -l)
              echo "第 $page 页找到 $REPO_COUNT 个 fork 仓库"
            else
              echo "第 $page 页没有 fork 仓库"
            fi

            LENGTH=$(echo "$BODY" | jq '. | length')
            if [ "$LENGTH" -lt 100 ]; then
              echo "✅ 到达最后一页"
              break
            fi

            page=$((page + 1))
            if [ $page -gt 5 ]; then
              echo "⚠️ 已获取最多 500 个仓库"
              break
            fi
            sleep 1
          done

          if [ -n "$ALL_FORK_REPOS" ]; then
            ALL_FORK_REPOS=${ALL_FORK_REPOS%,}
            TOTAL_COUNT=$(echo "$ALL_FORK_REPOS" | tr ',' '\n' | sed '/^$/d' | wc -l)
            echo "fork_repositories=$ALL_FORK_REPOS" >> $GITHUB_OUTPUT
            echo "✅ 共找到 $TOTAL_COUNT 个 fork 仓库"
          else
            echo "fork_repositories=" >> $GITHUB_OUTPUT
            echo "⚠️ 未找到任何 fork 仓库"
          fi

      - name: 准备仓库列表
        id: prepare-repos
        run: |
          MODE="${{ steps.determine-mode.outputs.mode }}"
          USER_INPUT="${{ github.event.inputs.repositories }}"

          if [ "$MODE" = "all" ]; then
            FINAL_REPOS="${{ steps.get-all-fork-repos.outputs.fork_repositories }}"
            if [ -z "$FINAL_REPOS" ]; then
              FINAL_REPOS="${{ github.repository }}"
            fi
          elif [ "$MODE" = "current" ]; then
            FINAL_REPOS="${{ github.repository }}"
          else
            FINAL_REPOS="$USER_INPUT"
          fi

          FINAL_REPOS=$(echo "$FINAL_REPOS" | tr -d ' ' | sed 's/,,*/,/g' | sed 's/^,//' | sed 's/,$//')
          echo "repositories=$FINAL_REPOS" >> $GITHUB_OUTPUT
          REPO_COUNT=$(echo "$FINAL_REPOS" | tr ',' '\n' | sed '/^$/d' | wc -l)
          echo "repo_count=$REPO_COUNT" >> $GITHUB_OUTPUT

      - name: 更新仓库 (API 同步)
        id: update-repos
        env:
          REPOSITORIES: ${{ steps.prepare-repos.outputs.repositories }}
        run: |
          echo "🔄 开始通过 GitHub API 同步仓库..."
          echo "仓库列表: $REPOSITORIES"
          echo "总仓库数: ${{ steps.prepare-repos.outputs.repo_count }}"

          if [ -z "$REPOSITORIES" ]; then
            echo "❌ 错误：未指定要同步的仓库"
            exit 1
          fi

          UPDATED_REPOS=0
          UP_TO_DATE_REPOS=0
          SKIPPED_REPOS=0
          FAILED_REPOS=0
          SKIPPED_REPO_NAMES=""
          FAILED_REPO_NAMES=""

          TOTAL_REPOS="${{ steps.prepare-repos.outputs.repo_count }}"

          for TARGET_REPO in $(echo "$REPOSITORIES" | tr ',' ' '); do
            TARGET_REPO=$(echo "$TARGET_REPO" | xargs)
            if [ -z "$TARGET_REPO" ]; then continue; fi

            echo ""
            echo "========================================"
            CURRENT_INDEX=$((UPDATED_REPOS + UP_TO_DATE_REPOS + SKIPPED_REPOS + FAILED_REPOS + 1))
            echo "🔄 同步仓库 ($CURRENT_INDEX/$TOTAL_REPOS): $TARGET_REPO"
            echo "========================================"

            REPO_INFO=$(curl -s -m 15 --retry 3 -H "Authorization: token $GH_TOKEN" \
                          "https://api.github.com/repos/$TARGET_REPO")

            ERROR_MSG=$(echo "$REPO_INFO" | jq -r '.message // empty')
            if [ -n "$ERROR_MSG" ] && [ "$ERROR_MSG" != "null" ]; then
              echo "❌ 获取仓库信息失败: $ERROR_MSG"
              FAILED_REPOS=$((FAILED_REPOS + 1))
              FAILED_REPO_NAMES="${FAILED_REPO_NAMES}\`$TARGET_REPO\`, "
              continue
            fi

            IS_FORK=$(echo "$REPO_INFO" | jq -r '.fork // false')
            if [ "$IS_FORK" != "true" ]; then
              echo "⚠️ $TARGET_REPO 不是 fork 仓库，跳过"
              SKIPPED_REPOS=$((SKIPPED_REPOS + 1))
              SKIPPED_REPO_NAMES="${SKIPPED_REPO_NAMES}\`$TARGET_REPO\`, "
              continue
            fi

            DEFAULT_BRANCH=$(echo "$REPO_INFO" | jq -r '.default_branch // "main"')
            echo "🌿 使用分支: $DEFAULT_BRANCH"

            API_RESPONSE=$(curl -s -m 15 --retry 3 -w "\n%{http_code}" \
              -X POST \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: token $GH_TOKEN" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/$TARGET_REPO/merge-upstream" \
              -d "{\"branch\":\"$DEFAULT_BRANCH\"}")

            HTTP_CODE=$(echo "$API_RESPONSE" | tail -n1)
            RESPONSE_BODY=$(echo "$API_RESPONSE" | sed '$d')

            echo "📊 API 响应状态码: $HTTP_CODE"

            case $HTTP_CODE in
              200)
                MERGE_TYPE=$(echo "$RESPONSE_BODY" | jq -r '.merge_type // "fast-forward"' 2>/dev/null || echo "fast-forward")
                if [ "$MERGE_TYPE" = "none" ]; then
                  echo "⏭️ 已是最新: (无需同步)"
                  UP_TO_DATE_REPOS=$((UP_TO_DATE_REPOS + 1))
                else
                  echo "✅ 同步成功 ($MERGE_TYPE)"
                  UPDATED_REPOS=$((UPDATED_REPOS + 1))
                fi
                ;;
              204|422)
                echo "⏭️ 已是最新"
                UP_TO_DATE_REPOS=$((UP_TO_DATE_REPOS + 1))
                ;;
              409)
                echo "⚠️ 存在合并冲突，需要手动解决"
                SKIPPED_REPOS=$((SKIPPED_REPOS + 1))
                SKIPPED_REPO_NAMES="${SKIPPED_REPO_NAMES}\`$TARGET_REPO\`, "
                ;;
              403|404)
                ERROR_MSG=$(echo "$RESPONSE_BODY" | jq -r '.message // "权限或仓库不存在"')
                echo "❌ 同步失败 ($HTTP_CODE): $ERROR_MSG"
                FAILED_REPOS=$((FAILED_REPOS + 1))
                FAILED_REPO_NAMES="${FAILED_REPO_NAMES}\`$TARGET_REPO\`, "
                ;;
              *)
                echo "❓ 未知响应 ($HTTP_CODE)"
                FAILED_REPOS=$((FAILED_REPOS + 1))
                FAILED_REPO_NAMES="${FAILED_REPO_NAMES}\`$TARGET_REPO\`, "
                ;;
            esac

            sleep 1
          done

          SKIPPED_REPO_NAMES=$(echo "$SKIPPED_REPO_NAMES" | sed 's/, $//')
          FAILED_REPO_NAMES=$(echo "$FAILED_REPO_NAMES" | sed 's/, $//')

          echo ""
          echo "🎉 API 同步完成！"
          echo "📊 统计："
          echo " ✅ 成功同步: $UPDATED_REPOS"
          echo " ⏭️ 已是最新: $UP_TO_DATE_REPOS"
          echo " 🔄 跳过: $SKIPPED_REPOS"
          echo " ❌ 失败: $FAILED_REPOS"
          if [ -n "$SKIPPED_REPO_NAMES" ]; then
            echo " 🔄 跳过仓库: $SKIPPED_REPO_NAMES"
          fi
          if [ -n "$FAILED_REPO_NAMES" ]; then
            echo " ❌ 失败仓库: $FAILED_REPO_NAMES"
          fi

          echo "updated_repos=$UPDATED_REPOS" >> $GITHUB_OUTPUT
          echo "up_to_date_repos=$UP_TO_DATE_REPOS" >> $GITHUB_OUTPUT
          echo "skipped_repos=$SKIPPED_REPOS" >> $GITHUB_OUTPUT
          echo "failed_repos=$FAILED_REPOS" >> $GITHUB_OUTPUT

          echo "skipped_repo_names<<EOF" >> $GITHUB_OUTPUT
          echo "$SKIPPED_REPO_NAMES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "failed_repo_names<<EOF" >> $GITHUB_OUTPUT
          echo "$FAILED_REPO_NAMES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: 生成报告
        if: always()
        env:
          MODE: ${{ steps.determine-mode.outputs.mode }}
          REPO_COUNT: ${{ steps.prepare-repos.outputs.repo_count }}
          UPDATED_REPOS: ${{ steps.update-repos.outputs.updated_repos || 0 }}
          UP_TO_DATE_REPOS: ${{ steps.update-repos.outputs.up_to_date_repos || 0 }}
          SKIPPED_REPOS: ${{ steps.update-repos.outputs.skipped_repos || 0 }}
          FAILED_REPOS: ${{ steps.update-repos.outputs.failed_repos || 0 }}
          SKIPPED_REPO_NAMES: ${{ steps.update-repos.outputs.skipped_repo_names || '' }}
          FAILED_REPO_NAMES: ${{ steps.update-repos.outputs.failed_repo_names || '' }}
        run: |
          echo "## 📊 仓库更新报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**账号:** $GITHUB_REPOSITORY_OWNER" >> $GITHUB_STEP_SUMMARY
          echo "**更新时间:** $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S') (北京时间)" >> $GITHUB_STEP_SUMMARY
          echo "**触发方式:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**更新模式:** $MODE" >> $GITHUB_STEP_SUMMARY
          echo "**目标仓库数:** $REPO_COUNT 个" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ job.status }}" = "success" ]; then
            echo "**状态:** ✅ 更新任务执行完成" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📈 更新统计" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| 状态 | 仓库数 | 说明 |" >> $GITHUB_STEP_SUMMARY
            echo "|------|--------|------|" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ 成功同步 | $UPDATED_REPOS | 通过 GitHub API 同步了上游更新 |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏭️ 已是最新 | $UP_TO_DATE_REPOS | 所有分支已是最新 |" >> $GITHUB_STEP_SUMMARY

            DISPLAY_SKIPPED="${SKIPPED_REPO_NAMES:-无}"
            DISPLAY_FAILED="${FAILED_REPO_NAMES:-无}"

            echo "| 🔄 跳过仓库 | $SKIPPED_REPOS | $DISPLAY_SKIPPED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ 失败仓库 | $FAILED_REPOS | $DISPLAY_FAILED |" >> $GITHUB_STEP_SUMMARY
            echo "| 📦 总计 | $REPO_COUNT | 目标仓库总数 |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 🔄 下次自动更新时间 (北京时间)" >> $GITHUB_STEP_SUMMARY
            echo "- 07:20（早晨）" >> $GITHUB_STEP_SUMMARY
            echo "- 19:20（晚上）" >> $GITHUB_STEP_SUMMARY
          else
            echo "**状态:** ❌ 更新任务执行失败" >> $GITHUB_STEP_SUMMARY
            echo "请查看详细日志了解错误信息。" >> $GITHUB_STEP_SUMMARY
          fi

# ══════════════════════════════════════════════
# 配置说明
# ══════════════════════════════════════════════
# 1. 创建 PAT：Settings → Developer settings → Personal access tokens
#    建议权限：repo
#
# 2. 添加 Secret：本仓库 → Settings → Secrets → Actions
#    名称固定为 AUTH_PAT_<账号名大写>,例如账号 timeflysoon → AUTH_PAT_TIMEFLYSOON,
#    账号 88alt → AUTH_PAT_88ALT
#    (脚本用 $GITHUB_REPOSITORY_OWNER 自动识别当前账号并拼出这个名字,同一份文件
#     部署到任意账号的仓库都无需改动)
#
# 3. 不触发其他仓库的 Actions，直接调用 GitHub 官方API：POST /repos/{owner}/{repo}/merge-upstream，
#    这就是网页上"Sync fork"按钮的底层机制，服务器端直接 fast-forward 或 merge 默认分支，不会
#    产生 push 事件，因此绝对不会触发 fork 仓库里的 on: push workflows。
#
# 4. API只在有更新时才修改分支，冲突时自动返回409并跳过，不会强制合并，无更新时返回422"已是最新"。
#
# 5. 手动模式：留空只同步当前仓库；输入all同步所有fork；也可指定 owner/repo1,owner/repo2。
#
# 6. 定时任务默认按 all 模式全量同步，每天两次（北京时间 07:20 和 19:20）。
#
# 7. 参考：https://github.com/wei/pull 是第三方开源 GitHub App，通过创建并合并 PR 同步 upstream，
#    但会触发 push / pull_request 事件，可能连带执行 fork 仓库里的 Actions，本方案刻意避免了这点。
# ══════════════════════════════════════════════
